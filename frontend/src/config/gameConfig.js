// Centralized gameplay timing/config values
// Update these to change behavior everywhere.

export const RECAP_DELAY_SECONDS = 5; // seconds to show the recap modal between questions
export const FINAL_DELAY_SECONDS = 0; // seconds to show the final modal (0 = no auto-dismiss)